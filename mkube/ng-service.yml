apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 80
      nodePort: 30000
  type: LoadBalancer  # Change to NodePort or LoadBalancer if needed
